const UserDisconnect = {
    event: "disconnect",
    enabled: true,
    handler:
        function () {
            //check if the user is part of any rooms
        
        }
};

export default UserDisconnect;
